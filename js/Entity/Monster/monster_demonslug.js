"use strict";

import { MonsterWalk } from "./monster_typed.js";

export class DemonSlug extends MonsterWalk {
    
    constructor(rad, x, y, src, data) {
        super(rad, x, y, src, data);
    }
}